class LineItem {
    constructor(productId, quantity, username, uniqueId) {
        this.productId = productId
        this.quantity = quantity
        this.username = username
        this.uniqueId = uniqueId;
    }
}

var items = []